<UserControl x:Class="Diplom.TeacherDashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="900"
             Background="#F8F9FA">

    <UserControl.Resources>
        <!-- Стиль для карточек статистики -->
        <Style x:Key="StatCardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="20"/>
        </Style>

        <!-- Стиль для карточки расписания -->
        <Style x:Key="ScheduleCardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="0"/>
        </Style>

        <!-- Стиль для элемента расписания -->
        <Style x:Key="ScheduleItemStyle" TargetType="Border">
            <Setter Property="Background" Value="#F8F9FA"/>
            <Setter Property="CornerRadius" Value="6"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>

        <!-- Статистика -->
        <Style x:Key="StatValueStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="28"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#5271FF"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="StatLabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="#666"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,6,0,0"/>
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="30,20">
        <StackPanel>

            <!-- Приветствие -->
            <StackPanel Margin="0,0,0,25">
                <TextBlock x:Name="GreetingText" FontSize="22" FontWeight="SemiBold" Foreground="#333"/>
                <TextBlock x:Name="TodayDateText" FontSize="13" Foreground="#999" Margin="0,5,0,0"/>
            </StackPanel>

            <!-- Статистика -->
            <TextBlock Text="📊 Статистика" FontSize="16" FontWeight="SemiBold" Foreground="#333" Margin="0,0,0,15"/>

            <Grid Margin="0,0,0,25">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Занятия сегодня -->
                <Border Grid.Column="0" Style="{StaticResource StatCardStyle}" Margin="0,0,10,0">
                    <StackPanel>
                        <TextBlock Text="📚" FontSize="24" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                        <TextBlock x:Name="TodayLessonsCount" Text="0" Style="{StaticResource StatValueStyle}"/>
                        <TextBlock Text="занятий сегодня" Style="{StaticResource StatLabelStyle}"/>
                    </StackPanel>
                </Border>

                <!-- Студенты -->
                <Border Grid.Column="1" Style="{StaticResource StatCardStyle}" Margin="10,0,10,0">
                    <StackPanel>
                        <TextBlock Text="👥" FontSize="24" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                        <TextBlock x:Name="TotalStudentsCount" Text="0" Style="{StaticResource StatValueStyle}"/>
                        <TextBlock Text="всего студентов" Style="{StaticResource StatLabelStyle}"/>
                    </StackPanel>
                </Border>

                <!-- Группы -->
                <Border Grid.Column="2" Style="{StaticResource StatCardStyle}" Margin="10,0,10,0">
                    <StackPanel>
                        <TextBlock Text="🏫" FontSize="24" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                        <TextBlock x:Name="TotalGroupsCount" Text="0" Style="{StaticResource StatValueStyle}"/>
                        <TextBlock Text="групп" Style="{StaticResource StatLabelStyle}"/>
                    </StackPanel>
                </Border>

                <!-- Средняя успеваемость -->
                <Border Grid.Column="3" Style="{StaticResource StatCardStyle}" Margin="10,0,0,0">
                    <StackPanel>
                        <TextBlock Text="⭐" FontSize="24" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                        <TextBlock x:Name="AverageGrade" Text="0.0" Style="{StaticResource StatValueStyle}"/>
                        <TextBlock Text="средний балл" Style="{StaticResource StatLabelStyle}"/>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Расписание на сегодня -->
            <Border Style="{StaticResource ScheduleCardStyle}" Margin="0,0,0,25">
                <StackPanel>
                    <Border Background="#F8F9FA" Padding="15,12" CornerRadius="8,8,0,0" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
                        <Grid>
                            <TextBlock Text="📅 Расписание на сегодня" FontSize="15" FontWeight="SemiBold" Foreground="#333"/>
                            <Button x:Name="ViewFullScheduleButton" 
                                    Content="Полное расписание →" 
                                    Background="Transparent"
                                    BorderThickness="0"
                                    Foreground="#5271FF"
                                    FontSize="12"
                                    Cursor="Hand"
                                    HorizontalAlignment="Right"
                                    Click="ViewFullSchedule_Click"/>
                        </Grid>
                    </Border>

                    <StackPanel x:Name="TodaySchedulePanel" Margin="15">
                        <TextBlock x:Name="NoLessonsText" 
                                   Text="Нет занятий на сегодня" 
                                   FontSize="13" 
                                   Foreground="#999"
                                   HorizontalAlignment="Center"
                                   Margin="0,20,0,20"
                                   Visibility="Collapsed"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Две колонки: быстрое действия и последние оценки -->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Быстрые действия -->
                <Border Grid.Column="0" Style="{StaticResource ScheduleCardStyle}" Margin="0,0,10,0">
                    <StackPanel>
                        <Border Background="#F8F9FA" Padding="15,12" CornerRadius="8,8,0,0" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
                            <TextBlock Text="⚡ Быстрые действия" FontSize="15" FontWeight="SemiBold" Foreground="#333"/>
                        </Border>

                        <StackPanel Margin="15">
                            <Button x:Name="MarkAttendanceButton" 
                                    Content="✅ Отметить посещаемость"
                                    Height="45"
                                    Margin="0,0,0,10"
                                    Style="{StaticResource PrimaryButton}"
                                    Click="MarkAttendance_Click"/>

                            <Button x:Name="AddGradesButton" 
                                    Content="📝 Выставить оценки"
                                    Height="45"
                                    Margin="0,0,0,10"
                                    Style="{StaticResource SecondaryButton}"
                                    Click="AddGrades_Click"/>

                            <Button x:Name="AddHomeworkButton" 
                                    Content="📚 Добавить домашнее задание"
                                    Height="45"
                                    Style="{StaticResource SecondaryButton}"
                                    Click="AddHomework_Click"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- Последние выставленные оценки -->
                <Border Grid.Column="1" Style="{StaticResource ScheduleCardStyle}" Margin="10,0,0,0">
                    <StackPanel>
                        <Border Background="#F8F9FA" Padding="15,12" CornerRadius="8,8,0,0" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
                            <TextBlock Text="🔄 Последние действия" FontSize="15" FontWeight="SemiBold" Foreground="#333"/>
                        </Border>

                        <StackPanel x:Name="RecentActivitiesPanel" Margin="15" MinHeight="150">
                            <TextBlock x:Name="NoActivitiesText" 
                                       Text="Нет недавних действий" 
                                       FontSize="13" 
                                       Foreground="#999"
                                       HorizontalAlignment="Center"
                                       Margin="0,20,0,20"
                                       Visibility="Collapsed"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </Grid>
        </StackPanel>
    </ScrollViewer>
</UserControl>